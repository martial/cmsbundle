<h1>Config list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Sitename</th>
            <th>Gg_email</th>
            <th>Gg_password</th>
            <th>Gg_analyticsid</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('config_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.sitename }}</td>
            <td>{{ entity.ggemail }}</td>
            <td>{{ entity.ggpassword }}</td>
            <td>{{ entity.gganalyticsid }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('config_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('config_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('config_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
