<h1>Node creation</h1>

<form action="{{ path('backend_node_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_node') }}">
            Back to the list
        </a>
    </li>
</ul>
